# Supabase Configuration (Required)
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Cross-Domain Authentication (Required for SSO)
# Set this to your root domain to enable session sharing across subdomains
# Examples:
#   - Production: .example.com (shares between auth.example.com and console.example.com)
#   - Local dev: localhost
# If not set, defaults to current hostname (single domain only)
VITE_COOKIE_DOMAIN=.example.com

# Cloudflare Turnstile CAPTCHA (Optional)
# Sign up at: https://www.cloudflare.com/products/turnstile/
# Enable in site.config.ts: auth.turnstile.enabled = true
VITE_TURNSTILE_SITE_KEY=
